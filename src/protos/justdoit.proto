syntax = "proto3";

option csharp_namespace = "BackendVue";

package justdoit;

service JustDoIt {
  rpc AddIssue  (TaskName) returns (Task);
}

message TaskName {
  string name = 1;
}

message Task {
  int32 id = 1;
  string name = 2;
  bool checked = 3;
}